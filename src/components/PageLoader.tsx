export default function PageLoader() {
  return (
    <div className="flex items-center justify-center min-h-screen w-full bg-sink-deep">
      <div className="w-8 h-8 border-2 border-sink-mint/20 border-t-sink-mint rounded-full animate-spin" />
    </div>
  );
}
